{
  "name": "yomlog",
  "version": "0.1.0",
  "scripts": {
    "postinstall": "npm install --prefix frontend --production=false",
    "build": "npm run build --prefix frontend",
    "serve": "npm run serve --prefix frontend",
    "install": "npm install --prefix frontend"
  }
}